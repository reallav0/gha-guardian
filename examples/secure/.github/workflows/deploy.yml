name: secure-deploy

on:
  push:
    branches: [main]

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Configure cloud auth with OIDC
        run: echo "OIDC exchange would happen here"
      - name: Deploy
        run: echo "Deploying trusted main branch"
